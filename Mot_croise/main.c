#include <stdlib.h>
#include <stdio.h>

#include <header.h>

int main() {

    char x;

    printf("Les mots sont croisée, appuie sur x pour les décroiser");
    scanf("%c", x);
    if (x = 'x') {
        printf("Les mots ne sont plus croisées");
    }

    return 0;
}
